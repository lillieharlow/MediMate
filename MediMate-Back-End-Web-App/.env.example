NODE_ENV=            # environment: development, test, or production
PORT=                # e.g. 7000
MONGODB_URI=         # e.g. mongodb://mongo:port/name
JWT_SECRET=          # secret for JWT signing, use a strong random value